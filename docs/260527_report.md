# 2026-05-27 작업 리포트

목표: **LLM·Playwright 연결 직전 단계까지 전부 동작** (이슈 → 러프 PR 자동 생성 포함).
결과: ✅ 풀 E2E 통과 — 폼 제출 → GitHub 이슈 + 시트 → 웹훅 → **자동 러프 PR 생성** → 시트 '진행중' (이슈 #26 → PR #27 검증 후 정리).

---

## A. 내가 임의로 정한 사항 (검토 후 바꾸라고 지시 주세요)

| # | 결정 | 내용 / 이유 | 바꾸려면 |
|---|---|---|---|
| A1 | **PR 본문 = 러프 안내** | "코드 없음(빈 PR) + Playwright/LLM 후속" + `관련 이슈 #N`. `Closes`는 병합 시 이슈 자동종료라 **일부러 안 씀**. | 문구/링크 방식 지정 |
| A2 | **자동 PR 브랜치 = `auto/issue-N`** | 빈 커밋 1개로 브랜치 생성 → 빈 PR. 같은 head PR 있으면 재사용(웹훅 재전송 멱등). | 브랜치 네이밍 규칙 |
| A3 | **`priority:P4` 버그는 자동 PR 제외** | Notion 태스크 "bug only, P4 제외" 의도 반영. | P4도 PR 생성 원하면 알려주기 |
| A4 | **시트 기록 시점 = `POST /issues`** | 웹훅 issues.opened는 중복 기록 대신 **PR 트리거만** 담당(이중기록 방지). | 웹훅 단일화 원하면 |
| A5 | **PR 상태 → 시트 '처리 상태'로만 반영** | 시트에 PR 전용 컬럼(PR번호/URL)이 없음. PR 생성=진행중, 병합=완료. | PR 컬럼 추가할지 결정 |
| A6 | **`create_empty_pr(issue, title, body)` 시그니처** | LLM 결과(AnalysisResult)에 비의존하게 일반화. LLM은 내일 본문 포맷만 채움. | (구조 결정, 변경 불필요할 듯) |
| A7 | **로그인 이름 = 명단(xlsx) 값 사용** | 입력 이름은 확인용, 검증/세션은 이메일 기준. | 입력 이름까지 일치 검증 원하면 |
| A8 | **웹훅 핸들러 예외 → 200 ACK 유지** | 핸들러 실패가 GitHub 재전송 폭주를 부르지 않게 로깅 후 200. | (운영 안전, 변경 불필요할 듯) |
| A9 | **CORS = `localhost:3000`만** | Phase A 로컬. (config `cors_origins`) | 배포 도메인 추가 시 |
| A10 | **자가등록 = 누구나 가능(승인 없음)** | 요청대로 미등록자 → /register → Members.xlsx 추가. **단 "외부인 차단" 취지와 충돌.** | 운영 시 '승인제' 전환 검토 |

## B. 당신이 정해야 할 사항 (미해결)

1. **`.env` 경로 버그**: `MEMBERS_XLSX_PATH=apps/api/data/Members.xlsx` → apps/api에서 실행 시 경로 어긋남(`apps/api/apps/api/...`). 지금은 실행 시 env로 교정해 띄움. → `data/Members.xlsx`로 고칠지 결정.
2. **타깃 repo(Neolab-test/test) PR 폐기 확정** — App 설치 불가라 PR도 이 repo에 생성(ADR 채택). 추후 Neolab이 App 설치 승인하면 전환 가능.
3. **P4 버그 자동 PR 제외** 맞는지 (A3).
4. **시트 PR 전용 컬럼 추가** 여부 (A5).
5. **자가등록 정책** 오픈 vs 승인제 (A10).
6. **기존 초록 행(헤더 등) 흰색 정리** 여부 (신규 행은 흰색 처리됨).
7. **로그인 이름 일치 검증** 여부 (A7).

## C. 오늘 채택된 ADR (Notion)
- 버그 PR 생성을 **Playwright 재현 확인으로 게이팅** (내일 구현)
- **이슈 상태(접수/진행중/재현불가/완료 · 검토/미반영/반영) + P1~P4 우선순위**
- **베타 PR = 이 repo** (타깃 repo App 설치 불가)

## D. 내일 (사용자 계획)
- Playwright 재현 게이팅 연결 + LLM(Claude API)으로 PR 본문(원인 가설) 작성

## E. 미커밋 → 이번에 정리
- 오늘 작업은 브랜치 + 커밋 + PR로 정리 (CI: ruff/mypy/pytest 통과).

---

## F. 사용자 결정 반영 (2026-05-28 업데이트)
- ✅ **B1 .env 경로** — `MEMBERS_XLSX_PATH=data/Members.xlsx`로 사용자가 수정.
- ✅ **B2 PR 대상 repo 최종 = `kimjy-st/QA_test`** — 사용자가 전용 repo 생성 + App 설치. 이슈=이 repo, PR=QA_test. (E2E: 이슈 #29 → PR QA_test#1 확인)
- ✅ **A3 P4 버그도 PR 생성** — 웹훅 핸들러의 P4 제외 해제(테스트 갱신).
- ✅ **A10/B5 자가등록 = 오픈 유지** — 미등록자 /register → Members.xlsx 추가.
- ✅ **B6 기존 시트 서식** — 헤더(1행) 초록 유지, 2행~ 흰색 처리.
- ⏳ **A7/B7 로그인 인증 강화** — 검토중. (이름 일치 검증 / 이메일 코드 인증 중 택)
- ⏳ **A5/B4 시트 PR 전용 컬럼** — 추가 여부 검토중.
- ⏳ **이미지 업로드(백엔드)** — 첨부 UI(Ctrl+V)는 완료, 실제 업로드/저장은 대기.
